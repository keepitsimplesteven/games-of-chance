{
  "name": "@games-of-chance/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/room.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "npx partykit dev",
    "deploy": "npx partykit deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@games-of-chance/shared": "workspace:*",
    "partykit": "^0.0.115"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
