{
  "name": "games-of-chance",
  "version": "0.1.0",
  "private": true,
  "description": "Live multiplayer browser-based games of chance",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @games-of-chance/server --filter @games-of-chance/client -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "deploy:client": "pnpm --filter @games-of-chance/client build",
    "deploy:server": "pnpm --filter @games-of-chance/client build && npx partykit deploy --config partykit.deploy.json"
  },
  "engines": {
    "node": ">=18"
  }
}
